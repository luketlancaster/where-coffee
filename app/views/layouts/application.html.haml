!!!
%html
  %head
    %meta{:content => "text/html; charset=UTF-8", "http-equiv" => "Content-Type"}/
    %title Where's the Coffee?!
    = stylesheet_link_tag     'application', media: 'all'
    = javascript_include_tag  'application'
    = csrf_meta_tags
    <link href='http://fonts.googleapis.com/css?family=Lato:400,900,400italic' rel='stylesheet' type='text/css'>
  %body
    #container
      %nav
        %ul
          %li= link_to "Home", root_path
          - if !logged_in?
            %li= link_to "Sign Up", new_user_path
            %li= link_to "Sign In", new_user_session_path
          - else
            %li#sign_out= link_to "Sign Out", user_session_path, method: :delete
    %section#content
      - flash.each do |key, message|
        %div{ class: "flash-#{key}" }= message
      %section= yield
    %footer
      %p Where Coffee
      = link_to "Source Code", "https://github.com/luketlancaster/where-coffee"
